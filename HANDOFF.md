# 交接：默哀报实机 Demo

把**本文件全文**交给本地 Grok Build，作为第一条指令。目标只有一件：把飞书真表跑通，并录一段不超过 3 分钟的实机 Demo。

用户本人看不懂飞书开放平台。你要**带着他一步一步点**，不要甩文档让他自己读。问一句、等他回一句、再给下一步。

---

## 0. 你是谁、做什么

你是 Grok Build。仓库已经在 GitHub：

https://github.com/19960705/moai-bao

产品：默哀报。飞书多维表格读项目 → Grok 写成讣告小报 → 发回飞书。

比赛硬性规则：飞书和 AI 必须同时参与**核心运行**。演示数据（复明日集团）只够看报，**过审无效**。

---

## 1. 完成标准（全部满足才算完）

- [ ] 停尸房里出现的是用户自己表里的项目，不是「增长引擎 3.0 / 复明日集团」那套演示尸体（除非用户故意把样例 CSV 导入了自己的表——那也算自己的表）。
- [ ] 点「让主笔写本周讣告」后，头版按这批真数据重排。
- [ ] 「发往飞书」至少成功一条：群机器人 **或** 云文档。
- [ ] 录到实机视频：先看报纸效果，再跑 抬尸 → 写报 → 发回飞书。不超过 3 分钟。

不要再做新功能、不要改设计、不要重写报纸。

---

## 2. 先问用户这 4 件事（缺一就停，问完再动手）

1. 能不能登录自己公司/个人的飞书网页版？（必须能）
2. 有没有权限在飞书里**创建企业自建应用**和**多维表格**？（没有就让他找管理员，你不要硬做）
3. 有没有一个可以加机器人的飞书群？没有就走云文档，不要卡在群上。
4. 默哀报现在是在本机 `npm run dev` 开着，还是只用 GitHub / 线上预览？

---

## 3. 飞书侧：你要带他点的最短路径

不要讲原理。每次只发 1～3 个按钮名。

### 3.1 建自建应用

1. 让用户打开 https://open.feishu.cn/app 并登录。
2. 点「创建企业自建应用」。应用名填 `默哀报`。
3. 进这个应用 → **凭证与基础信息** → 让他把 **App ID**（`cli_` 开头）和 **App Secret** 复制发给你。你先记在本地，不要写进 Git、不要 commmit。
4. **应用能力** → 打开 **机器人**。
5. **权限管理** → 开通并申请：
   - 查看、检索、获取多维表格中的数据（读记录）
   - 创建、编辑云文档（docx）
6. **版本管理** → 创建版本 → 申请开通，可见范围至少包含用户自己。等到「已开通」再往下。

搜不到应用、接口 403，多半是第 4～6 步没做完。

### 3.2 建多维表格

1. 飞书里新建「多维表格」，名字：`项目跟踪`。
2. 表头必须有这 5 列（名字一字不差，或之后在编辑部改成他的列名）：

| 列名 | 类型 |
|---|---|
| 项目名 | 文本 |
| 负责人 | 文本或人员 |
| 状态 | 文本或单选 |
| 最后更新 | 日期 |
| 截止日期 | 日期 |

3. 至少填 3 行能被判死的数据。直接让他导入仓库文件：

`public/guide/bitable-sample.csv`

或手填（状态/日期要「看起来已死」）：

- 增长引擎 3.0 / 小周 / 进行中 / 2026-05-26 / 2026-06-01
- 智能客服「小蜜」 / 产品汪 / 暂停 / 2026-04-12 / （空）
- 团建小程序 / 已离职 / 已上线 / 2025-12-18 / （空）

4. 浏览器打开这张表，地址类似：

`https://xxx.feishu.cn/base/ABC123?table=tblXYZ`

- `ABC123` = 编辑部里的 **多维表格 app_token**
- `tblXYZ` = 编辑部里的 **数据表 table_id**

让他把完整 URL 发给你，你来拆。

### 3.3 把应用加成这张表的协作者（不是「分享给人」）

加人用「分享」。加**应用**要用另一入口：

1. 打开这张多维表格。
2. 右上角 **`···`**（三点，不是「分享」）。
3. **更多** → **添加文档应用**。
4. 搜索 `默哀报`（和应用名一致）。
5. 权限选 **可阅读**。若表开了高级权限，选 **可管理**。
6. 添加。

搜不到：回 3.1，检查机器人已开、权限已开通、版本已发布。

### 3.4 写回飞书：两条里做一条就行

**优先 A：群机器人（更适合 Demo）**

1. 打开一个群 → 设置 → 群机器人 → 添加自定义机器人，名字 `默哀报`。
2. 复制 Webhook：`https://open.feishu.cn/open-apis/bot/v2/hook/…`
3. 发给你，填进编辑部「群机器人 Webhook」。

**否则 B：云文档**

1. 云空间建一个文件夹，例如 `默哀报出品`。
2. 文件夹链接里的 token 填进编辑部「云文档文件夹 token」。
3. 同样用「添加文档应用」把 `默哀报` 加成这个文件夹的协作者（可编辑）。

A、B 都没有，「发往飞书」会失败。抬尸 + 写报仍然能证明飞书在读、AI 在写；但 Demo 里尽量做成 A 或 B。

---

## 4. 默哀报侧：按按钮走

应用路由（本地一般是预览里这些路径）：

| 路径 | 顶栏 | 干什么 |
|---|---|---|
| `/` | 默哀报 | 报纸 |
| `/guide` | 指南 | 给人看的说明，评委页 |
| `/login` | 入内 | 注册/登录 |
| `/morgue` | 停尸房 | 从飞书抬尸 |
| `/press` | 编辑部 | 填钥匙、写报、发报 |

### 4.1 入内

1. 顶栏点 **入内**。
2. 注册一个账号（邮箱或页面上已有的登录方式，用一个就行）。
3. 登录成功，顶栏不再显示「入内」。

### 4.2 编辑部填钥匙

打开 `/press`。对照填（密钥不要进 Git）：

- App ID = `cli_…`
- App Secret = 开放平台那串
- 多维表格 app_token = URL 里 `/base/` 后面那段
- 数据表 table_id = URL 里 `table=` 后面那段
- 项目名列 = `项目名`
- 负责人列 = `负责人`
- 状态列 = `状态`
- 最后更新列 = `最后更新`
- 截止日期列 = `截止日期`
- 群机器人 Webhook = 有就填；没有留空
- 云文档文件夹 token = 有就填；没有留空

点 **保存钥匙**。应出现「已接入」。

### 4.3 停尸房抬尸

打开 `/morgue`，点从飞书抬出（文案是「从飞书抬出」一类）。

成功：toast 类似「抬出 N 具，法医认定 M 具死亡」，列表是用户表里的项目名。

失败：把 toast / 报错原文留下来。常见原因：

- 没加文档应用 → 权限 denied
- token / table_id 填反
- 应用版本未开通
- 列名和表头不一致

**禁止**在失败后用演示数据冒充成功。

### 4.4 主笔写报

回 `/press`，点 **让主笔写本周讣告**。成功后会回到头版，headline / 讣告应是这批真项目，不是写死的演示稿（若用户导入的就是样例 CSV，内容会很像演示，但报纸底部或停尸房应标明来源是飞书）。

### 4.5 发往飞书

同页点 **发往飞书**。

- 填了 Webhook：去那个群看是否出现讣告。
- 填了文件夹 token：去云空间看是否多了一份文档。

两条都失败也可以先录「抬尸 + 写报」；同时修权限再补一段发报。

---

## 5. 实机 Demo 录屏脚本（≤ 3 分钟）

必须是**真界面操作**，不要用仓库里那条 59 秒预告片当正片（预告片可当片头 5 秒）。

建议结构：

0:00–0:20 打开头版，口头一句：默哀报每周把飞书里死掉的项目写成讣告。  
0:20–0:50 切到飞书，打开《项目跟踪》真表，滚动几行。  
0:50–1:20 停尸房 → 从飞书抬出，停 2 秒让人看清项目名。  
1:20–2:00 编辑部 → 让主笔写本周讣告 → 回到头版，点开一篇验尸报告。  
2:00–2:40 发往飞书 → 切到飞书群或云文档，证明写回。  
2:40–3:00 结束语：飞书负责抬尸收报，Grok 负责验尸。读完请默哀三秒。

画面不要只念 PPT。社媒发布时带 `#飞书AI绝活大会`。

---

## 6. 死亡判定（用来解释为什么某行被抬出来）

满足任一即死：

- 状态含：暂停、取消、已上线、评审、归档、废弃、每周复活
- 最后更新距今 ≥ 21 天
- 截止日期逾期 ≥ 7 天，且状态不是完成 / 关闭
- 负责人为空、无主、或含「离职」，且沉默 ≥ 14 天

---

## 7. 禁止

- 不要把 App Secret 写进仓库、README、截图、聊天记录的 Git 提交。
- 不要为了「看起来能跑」把演示数据说成飞书数据。
- 不要重做 UI、不要加新功能、不要改报名规则。
- 用户卡住时，一次只问一个问题。

---

## 8. 你做完后回用户的话术

「真表已经抬出来了，头版是按你的飞书项目写的。发往飞书：群 / 文档（写哪条成功了）。下一步按第 5 节录 3 分钟，发小红书或 B 站，带 #飞书AI绝活大会。」
